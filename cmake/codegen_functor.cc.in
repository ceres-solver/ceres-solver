// This file is generated by Ceres autodiff code generation."
// http://ceres-solver.org/

#include "ceres/codegen/autodiff.h"
#include "@COST_FUNCTOR_INPUT_FILE@"
#include <fstream>
#include <iostream>
#include <string>
#include <vector>

int main() {
  const std::vector<std::string> lines =
      ceres::GenerateCodeForFunctor<@COST_FUNCTOR_NAME@>(
          ceres::AutoDiffCodeGenOptions());
  const std::string output_file = "@FULL_OUTPUT_FILE@";
  std::ofstream stream(output_file);
  if(!stream.is_open())
  {
    std::cout << "Could not open file " << output_file << std::endl;
    return 1;
  }
  for (auto line : lines) {
    stream << line << std::endl;
  }
  return 0;
}
