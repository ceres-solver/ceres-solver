// This file is generated by Ceres autodiff code generation."
// http://ceres-solver.org/

#include "ceres/codegen/autodiff.h"
#include "@COST_FUNCTOR_INPUT_FILE@"
#include <fstream>

int main() {
  std::vector<std::string> code =
      ceres::GenerateCodeForFunctor<@COST_FUNCTOR_NAME@, @COST_FUNCTOR_NUM_RESIDUALS@, @COST_FUNCTOR_NUM_PARAMETERS_STR@>(
          ceres::AutoDiffCodeGenOptions());
  const std::string output_file = "@FULL_OUTPUT_FILE@";
  std::ofstream strm(output_file);
  if(!strm.is_open())
  {
    std::cout << "Could no open file " << output_file << std::endl;
    return 1;
  }
  for (auto str : code) {
    strm << str << std::endl;
  }
  return 0;
}
